export const metadata = {
  title: 'Marketing',
  description: '',
}

export default function MarketingLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
