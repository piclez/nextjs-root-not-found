export const metadata = {
  title: 'Marketing',
  description: '',
}

const MarketingLayout = ({
  children
}: {
  children: React.ReactNode;
}) => {
  return (
    <div>
      {children}
    </div>
  )
}

export default MarketingLayout;
